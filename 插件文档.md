# 简易文本 · 使用说明（QQ 官方机器人）

本插件走 **kakake GF 官方车道**（目录名 `GF-plugin-mkjianyi`），对接 QQ 开放平台机器人，**不是** OneBot / NapCat 野生协议。

在 `文本类插件/` 下新建任意名称的 `.ka` 文件即可。目录名固定为 `文本类插件`。

可跑自测见同目录：

- [`基础例子1.ka`](文本类插件/基础例子1.ka)：通用 / Markdown / 按钮等（测试1…）
- [`基础例子2.ka`](文本类插件/基础例子2.ka)：群聊类（测试13～17）
- [`基础例子3.ka`](文本类插件/基础例子3.ka)：生命周期事件（进群 / 好友 / 通知开关 / 成员进退 / 进群申请）

### 手机端语法高亮（MT 管理员）

`数据文件/ka.mtsx` 是 **手机端 MT 管理员** 的语法高亮规则，**专门适用于 `.ka` 配置文件**（源文件在仓库 `syntax/ka.mtsx`，打包时写入 `数据文件/`）。在 MT 管理员中导入该规则后，编辑 `.ka` 可获得关键字 / 字符串 / 方括号内置值等着色。要求 **MT ≥ 2.16.0**。

---

## 底层核心（方括号，名称固定不可改）

### 内置值

| 写法 | 含义 |
|------|------|
| `[文本消息]` | 当前消息纯文本（已去掉 `<@openid>`）；**生命周期事件驱动时**等于 `[事件]` 的中文简短名 |
| `[事件]` | 当前事件的中文简短名（见下表）；普通群/私聊消息为 `群消息` / `私聊`，回调为 `互动` |
| `[群号]` | 本群 `group_openid`；私聊或取不到为 `0` |
| `[发言人]` | 发言人 openid；取不到为 `0` |
| `[获取群身份]` | 当前**发言人**群身份：`member` / `admin` / `owner`；取不到 `0` |
| `[获取群身份:自己]` | **机器人**在本群身份（`GET …/bot_state`，官方白名单接口；失败为 `0`） |
| `[获取群身份:发言人]` | 同默认 |
| `[获取群身份:变量]` / `[获取群身份:openid]` | 指定成员；仅当等于当前发言人或机器人 openid 时可解析，否则 `0`（群聊无通用查他人身份接口） |
| `[获取群信息]` | `GET …/info` 群基本信息回执：`成功`/`群名`/`简介`/`分类`/`人数`/`标签`/`错误`（白名单） |
| `[获取机器人群状态]` | `GET …/bot_state` 全量状态回执：`成功`/`openid`/`入群时间`/`主动消息`/`收消息设置`/`身份`/`错误`（白名单） |
| `[查询群禁言]` | `GET …/restrict_chat_setting`：`成功`/`全员模式`/`禁言人数`/`明细`/`错误` |
| `[AppID]` | 当前官方连接的机器人 AppID；取不到为 `0` |
| `[消息ID]` | 本条上报的消息 `id`；取不到为 `0` |
| `[取艾特数]` | 正文里 `<@…>` 数量 |
| `[取艾特]` / `[取艾特:N]` / `[取艾特:变量]` | 按索引取艾特 **openid**（默认 `0` 为首个）；索引不存在为 `0`；旧写法 `;` 仍兼容 |
| `[取括号]` / `[取括号:N]` | 取**最近一次** `.match`；按索引，默认 `0` 全文；`1` 起为捕获组；没有则为空 |
| `[时间]` | 当前本地时间，默认 `YYYY-MM-DD HH:mm:ss`（精确到秒） |
| `[时间:时间戳毫秒]` / `[时间:时间戳秒]` | 毫秒 / 秒时间戳 |
| `[时间:年]` `[时间:月]` `[时间:日]` `[时间:时]` `[时间:分]` `[时间:秒]` | 分量；月日时分秒两位补零 |
| `[时间:YYYY-MM-DD]` 等 | **自定义模板**；占位 `YYYY` `MM` `DD` `HH` `mm` `ss` 任选，其余原样 |
| `[转时间:时间戳毫秒,值]` / `[转时间:时间戳秒,值]` | 时间戳反转为文本；默认 `YYYY-MM-DD HH:mm:ss` |
| `[转时间:时间戳毫秒,值,"模板"]` | 自定义模板；占位 `YYYY` `MM` `DD` `HH` `mm` `ss`，其余原样 |
| `[插件定义:变量名]` | 读取你用 `[全局定义]` / `[临时定义]` 起的变量；**必须写在引号外** |

### 生命周期事件（`[事件]`）

由 `plugin_onevent` 接入；消息与按钮回调仍只走消息通道，不会双跑。

| `[事件]` | 官方 `t` | 能否 `发消息` |
|----------|----------|----------------|
| `进群` | `GROUP_ADD_ROBOT` | 能 |
| `退群` | `GROUP_DEL_ROBOT` | **否**（仅日志） |
| `加好友` | `FRIEND_ADD` | 能 |
| `删好友` | `FRIEND_DEL` | **否** |
| `开主动消息` | `C2C_MSG_RECEIVE` 或互动授权 `scope=c2c_push`+开 | 能 |
| `关主动消息` | `C2C_MSG_REJECT` 或互动授权关 | **否** |
| `开群通知` | `GROUP_MSG_RECEIVE` 或互动授权 `scope=group_push`+开 | 能 |
| `关群通知` | `GROUP_MSG_REJECT` 或互动授权关 | **否** |
| `成员进群` | `GROUP_MEMBER_ADD` | 能 |
| `成员退群` | `GROUP_MEMBER_REMOVE` | 能（失败只打日志） |
| `进群申请` | `GROUP_JOIN_REQUEST` | 能（需机器人为群管理员；见例子3） |
| `群消息` / `私聊` / `互动` | 消息 / 回调事件 | 走消息通道；`[文本消息]` 仍为正文或按钮 data |

匹配示例：

```text
[如果]([事件] == "进群"){ 发消息("我进群了") }
```

事件驱动时也可用 `[文本消息] == "进群"`（二者相同）。不可回复事件里写了 `发消息` 只会打日志、不会真正发出。

> 资料卡开关「主动消息」在现网常以 `INTERACTION_CREATE`（`type=18`，`authorize_data.scope=c2c_push`）上报：`switch=true` 为开，缺省/`false` 为关；脚本仍用 `[事件] == "开主动消息"` / `"关主动消息"` 匹配。

### 字符串插值

在 `"…"` 或反引号 `` `…` `` 里可直接插入名字（两种引号等价，含插值）：

| 写法 | 含义 |
|------|------|
| `{名字}` | 临时 / 全局变量，或同名内置（如 `{消息ID}` `{AppID}` `{发言人}` `{事件}`） |
| `${名字}` | 同上（JS 模板风格，效果相同） |
| `{取括号}` / `{取括号:N}` | 同 `[取括号]` |
| `{获取群身份}` / `{获取群身份:自己}` | 同 `[获取群身份…]` |
| `{取艾特}` / `{取艾特:N}` / `{取艾特:变量}` | 同 `[取艾特]`（旧 `;` 仍兼容） |
| `{时间}` / `{时间:年}` / `{时间:YYYY-MM-DD}` 等 | 同 `[时间]` |
| `{转时间:时间戳毫秒,值}` 等 | 同 `[转时间:…]` |
| `{表达式}` | 任意表达式，如 `{回执[消息id]}` `{1+2}` `{转数字(1+2)}` |
| `\{` | 字面 `{` |

```text
出 += "\n本条消息ID={消息ID}"
出 += `\n发言人={发言人}`
发消息(`[图片,url:https://q.qlogo.cn/qqapp/{AppID}/{发言人}/100]`)
发消息("括号内容={取括号:1}")
```

```text
[临时定义] 毫秒 = [时间:时间戳毫秒]
发消息([转时间:时间戳毫秒,毫秒])
发消息([转时间:时间戳毫秒,毫秒,"YYYY年MM月DD日，HH时mm分ss秒"])
```

方括号写法仍可用；新脚本优先用 `{}`。

### 结构

| 写法 | 含义 |
|------|------|
| `[风格标准]:text` | 文本风格声明 |
| `[如果](条件){ … }` | 条件块，可嵌套 |
| `}[反之]{ … }` | 条件不成立时执行 |
| `[For循环](条件){ … }` | 条件循环（有安全上限）；可用全角 `＜` `＞` |
| `[跳过]` | 跳过本轮循环剩余语句（continue） |
| `[中断]` | 结束**本层**循环（break，不是整脚本结束） |
| `[结束]` | 结束当前 `.ka` 本次执行 |
| `[等待]` | 等待本脚本里尚未等完的 `发消息` / `发详细消息` / `发Markdown` / `撤回`（语句版并行）全部完成 |
| `[等待] 发消息(…)` | 立刻 await 一次发送（不赋值）；`发Markdown` / `撤回` 同理 |
| `[等待] 变量名` | 若变量仍是进行中的 Promise 则 await 并写回回执；已是回执则无操作 |
| `[等待:毫秒]` | 延时指定毫秒后再继续，如 `[等待:2000]` 等 2 秒；也可写变量 `[等待:延时]` |

### 定义

| 写法 | 含义 |
|------|------|
| `[全局定义] 名字 = 值` | 脚本级变量（同文件共享，重载前保留） |
| `[临时定义] 名字 = 值` | 本次执行局部变量 |
| `名字 = 值` | 给**已有**变量再赋值（不必再写临时/全局定义；未定义过时按局部写入） |

赋值右侧可以是 `发消息(...)` / `发详细消息(...)` / `发Markdown(...)` / `撤回(...)`，也可以显式写 `[等待]`（推荐，语义更清楚）：

```text
[临时定义] 回执 = [等待] 发消息("hello")
[临时定义] 回执 = 发消息("hello")          # 同样会等回执（赋值即 await）
[临时定义] 撤 = [等待] 撤回(回执[消息id])
[临时定义] md = "# 标题
正文"
[临时定义] md回执 = [等待] 发Markdown(md)
```

回执对象字段（发送与撤回同形）：

| 字段 | 含义 |
|------|------|
| `成功` | 是否成功（布尔） |
| `消息id` | 发送：官方回执 id；撤回：被撤消息 id |
| `条数` | 发送：实际调用条数；撤回：成功 `1` / 失败 `0` |
| `错误` | 失败原因；成功时为空串 |

读取：`回执[消息id]`、`回执["成功"]`，或在字符串里 `{回执[消息id]}`。

```text
[临时定义] 回执 = [等待] 发消息("hello")
发消息("成功={回执[成功]} id={回执[消息id]} 条数={回执[条数]}")
# 也可用反引号 + 引号下标：`成功={回执["成功"]}`
# 或拼接：发消息("成功=" + 回执["成功"])

发消息("并行A")
发消息("并行B")
[等待]
发消息("都发完了")

[等待] 发消息("直接等待")
```

说明：

- **单独一行** `发消息(...)` / `发Markdown(...)` / `撤回(...)`：不阻塞后续语句，进入 pending；用 `[等待]` 或脚本结束时自动 flush
- **`[临时定义] x = [等待] 发消息(...)`** 或 **`x = 发消息(...)`**：赋值时已等完，`x` 直接是回执（`发Markdown` / `撤回` 同理）
- 字符串支持**跨行**（未闭合的 `"…"` / `` `…` `` 会自动合并）；MD 正文**推荐双引号**括住，避免与 `` `行内代码` `` 冲突；`\u200B` / `\uXXXX` 可用
- `[等待]` 既可单独成行，也可写在定义/表达式右侧当 await 前缀
- 同一次被动回复连发多条（含并行语句版）会自动递增官方 `msg_seq`，避免「消息被去重」
- 官方被动回复大约 **5 条**（次数/时效）；满额后或收到「被动回复…超过限制」时，会**自动去掉 `msg_id` 改为主动发送**（需机器人已开通主动消息；与 `发详细消息` 同类）

### 语句

| 写法 | 含义 |
|------|------|
| `发消息(内容)` | 回复当前会话（被动带 `msg_id`）；语句版不阻塞；可含 `[图片,…]` 等标记 |
| `发详细消息("群"\|"私", openid, 内容)` | 指定群 / 用户 openid 主动发送；语句版不阻塞 |
| `发Markdown(内容)` | 原生 Markdown（`msg_type=2`）；**不**解析富媒体标记；图片用 `![alt #Wpx #Hpx](url)`（缺尺寸时默认补 `#640px #360px`）；**不做**已淘汰的模版消息 |
| `发Markdown(内容, 键盘)` | 同上，并挂底部 **回调按钮**（`keyboard`）；自定义按钮官方可能需内邀开通 |
| `撤回(消息id)` | 撤回**当前会话**中的消息；语句版不阻塞；约 2 分钟内可撤自己发的；群内撤他人需管理权限 |

### 表达式函数

| 写法 | 含义 |
|------|------|
| `转数字(表达式)` | 去空白后转为 **number**；括号内 `+ - * /` 强制按数字算（字符串也可）；失败则返回普通求值的**原样字符串** |
| `设置群禁言(openid, 秒数)` | `POST …/restrict_chat_setting`；秒数>0 禁言至现在+秒（最长 30 天），`<=0` 解禁；回执 `成功`/`操作`/`成员`/`到期`/`错误`；需机器人为群管理员 |
| `取入群申请()` | 从当前 `GROUP_JOIN_REQUEST` 事件解析：`成功`/`申请ID`/`openid`/`昵称`/`申请时间`/`来源`/`邀请人`/`验证方式`/`问题`/`答案`/`问答明细`/`风险提示` 等 |
| `审批入群申请(openid, 操作, 申请ID?, 拒绝理由?)` | `POST …/approval_join_request/{openid}`；操作=`同意`/`拒绝`/`拉黑`（拒绝并 `add_to_member_blacklist`）；回执 `成功`/`操作`/`成员`/`申请ID`/`拉黑`/`错误`；需机器人为群管理员 |
| `撤回(消息id)` | 同语句版，可在表达式 / 赋值中使用，返回回执 |
| `发Markdown(内容)` | 同语句版，可在表达式 / 赋值中使用，返回回执 |
| `指令输入(填入内容, 显示文字?)` | 生成 Markdown **参数指令**蓝字：点击后把文本插入输入框（可再编辑发送） |
| `回车指令(发送内容, 显示文字?)` | 生成 Markdown **回车指令**蓝字：点击后直接发送；**群聊不支持**，建议私聊 |
| `回调钮(id, 文字, data, 样式?, 点后文字?, 权限?, 指定用户?)` | 底部 **回调**（`type=1`）；样式 `1`蓝/`2`白/`3`红；`点后文字`=`visited_label`；权限 `0`指定用户/`1`仅管理/`2`所有人（默认）/`3`身份组；权限`0`时传 openid |
| `跳转钮(id, 文字, url, 样式?, 点后文字?)` | 底部 **跳转**（`type=0`）；`url` 为 http / 小程序 scheme |
| `指令钮(id, 文字, data, 样式?, 点后文字?, 回车?, 引用?)` | 底部 **指令**（`type=2`）；`回车=1` 直接发送（群聊不支持）；`引用=1` 带引用 |
| `按钮行(...钮)` | 一行按钮；**同行均分宽度**，想齐宽就让每行按钮数相同 |
| `键盘(...行)` | 整盘 keyboard；也可把「行」直接作 `发Markdown` 第二参 |
| `访问("GET"\|"POST", 网址, …)` | HTTP 请求，见下节 |
| `取JSON(文本, 路径…?)` | 解析 JSON；可继续 `取JSON(文, 0, "name")` 或 `取JSON(文)[0][name]` |
| `取长度(值)` | 数组元素数 / 字符串长度 / 对象键数；也可直接传 JSON 文本 |

### 访问（HTTP）

对齐 Secluded `$访问` / MK `fetchAPI`。回执：`成功` / `状态码` / `内容`（应答正文） / `错误`。默认超时 15 秒。

| 写法 | 含义 |
|------|------|
| `访问("GET", 网址)` | GET |
| `访问("GET", 网址, 请求头)` | GET；请求头为对象或 JSON 字符串 |
| `访问("POST", 网址, 请求体)` | POST |
| `访问("POST", 网址, 请求体, 请求头)` | POST + 请求头 |

请求体：

- **对象** → `JSON.stringify`，默认 `Content-Type: application/json`（对齐 MK）
- **JSON 文本**（以 `{`/`[` 开头）→ 按 JSON 发
- **其它字符串**（如 `a=b&c=d`）→ 表单，默认 `application/x-www-form-urlencoded`（对齐 Secluded）

> 字符串里的 `{` / `}` 默认是插值，写 JSON 正文时请写成 `\{` / `\}`（推荐反引号包一层）。

```text
[临时定义] r = 访问("GET", "https://xn--mk-ub3cl61ae1v.xn--c5w857b.xn--fiqs8s/mkbot/赞助.json")
发消息("成功=" + r[成功] + "\n" + r[内容])

[临时定义] p = 访问("POST", "https://postman-echo.com/post", `\{"from":"mkjianyi","ping":1\}`)
发消息(p[内容])

[临时定义] f = 访问(
    "POST",
    "https://postman-echo.com/post",
    "cmd=login&username=123&password=123",
    `\{"cookie":"a=666"\}`
)
发消息(f[内容])
```

### 取JSON

对齐 Secluded `$JSON 解析`。把应答正文解析成对象/数组后按下标或键取值；解析失败返回 `""`。

| 写法 | 含义 |
|------|------|
| `取JSON(文本)` | 整段解析，结果可再 `[0]` / `[name]` |
| `取JSON(文本, 0, "name")` | 路径取值（数字当下标，字符串当键） |

```text
[临时定义] r = 访问("GET", "https://xn--mk-ub3cl61ae1v.xn--c5w857b.xn--fiqs8s/mkbot/赞助.json")
[等待] 发消息(r[内容])
[临时定义] 一 = 取JSON(r[内容], 0)
[等待] 发消息("name=" + 一[name] + " qq=" + 一[qq])

[临时定义] 名单 = 取JSON(r[内容])
[临时定义] n = 取长度(名单)
[临时定义] i = 0
[临时定义] 出 = "共" + n + "人："
[For循环](i ＜ n){
    [临时定义] 项 = 名单[i]
    出 += "\n[" + i + "] " + 项[name]
    i++
}
[等待] 发消息(出)
```

### 读 / 写 / 读文件 / 写文件（持久化）

对齐 MK 的 `readB`/`writeB`/`readA`/`writeA`。相对路径默认写在账号数据目录 `kakake/data/<账号>/GF-plugin-mkjianyi/`（**不是** `plugins_two`）。

| 写法 | 含义 |
|------|------|
| `读(路径, 键, 默认值?)` | 读 JSON 对象里一个键；缺文件/缺键 → 默认值（省略则为 `""`） |
| `写(路径, 键, 值)` | 合并写入 JSON 键；自动建目录；损坏 JSON 当 `{}`；成功 `true` / 失败 `false` |
| `读文件(路径)` | 整文件 UTF-8；不存在 → `""` |
| `写文件(路径, 内容)` | 整文件覆盖写；自动建目录；成功 `true` / 失败 `false` |

路径规则：

| 形式 | 落点 |
|------|------|
| 普通相对路径（如 `存档/积分.json`） | `dataPath`（账号可写目录） |
| `数据文件/…` 或 `默认资源/…` | `pluginPath`（插件副本内捆绑资源） |
| 绝对路径 | 原样使用 |

相对路径禁止 `../` 逃出对应根目录。可作赋值或单独一行语句。

```text
写("存档/积分.json", [发言人], 100)
[临时定义] 分 = 读("存档/积分.json", [发言人], 0)
写文件("日志/今日.txt", "hello")
[临时定义] 文 = 读文件("日志/今日.txt")
```

函数调用括号可跨行（未闭合的 `(` 会自动并成一句），长键盘建议一钮一行：

```text
[临时定义] 行1 = 按钮行(
    回调钮("a", "蓝", "点了蓝", 1),
    回调钮("b", "白", "点了白", 2),
    回调钮("c", "红", "点了红", 3)
)
发Markdown("# 回调", 键盘(行1))
```

```text
[临时定义] n = 转数字(1 + 2 * 3)     # → 7（数字）
[临时定义] n = 转数字(" 08 ")        # → 8
[临时定义] n = 转数字("10" * "2")    # → 20
[临时定义] n = 转数字("abc")         # → "abc"
发消息("结果={转数字(1+2)}")

[临时定义] md = "# 交互
点我填入：" + 指令输入("测试1", "填入测试1")
md += "\n点我直发：" + 回车指令("测试1", "点我直接发测试1")
md += "\n打开：[文档](https://bot.q.qq.com/wiki/)"
发Markdown(md)

发Markdown("# 回调\n点按钮", 键盘(按钮行(回调钮("1", "点我", "收到回调"))))
```

说明：算式写在括号表达式里；`"1+2"` 整段字符串**不会**再当公式解析。

Markdown **文本交互**（文内可点蓝字，写在 `发Markdown` 的 content 里）：

| 能力 | 写法 | 说明 |
|------|------|------|
| 参数指令 | `指令输入(填入, 显示?)` | 插入输入框 |
| 回车指令 | `回车指令(发送, 显示?)` | 直接发送；群不支持 |
| 跳转 | `[文字](https://…)` | 普通 MD 链接 |

Markdown **底部回调按钮**（不能嵌进正文，只能挂消息底部）：

| 能力 | 写法 | 说明 |
|------|------|------|
| 回调按钮 | `发Markdown(md, 键盘(按钮行(回调钮(...))))` | 点击不产生用户发言；触发 `INTERACTION_CREATE`，插件把 `button_data` 注入为 `[文本消息]` 再跑脚本；须 ACK |
| 跳转 / 指令钮 | `跳转钮` / `指令钮` | `type=0` 打开链接；`type=2` 填入或回车发送指令（回车群聊不支持） |
| 点后文案 / 权限 | `回调钮(..., 点后文字, 权限, 指定用户?)` | `0`指定用户 / `1`仅管理 / `2`所有人。**群聊**：官方 `type=1` 常不拦点击；插件会在**回调时按点击者身份**拦截。身份来源：凡成功上报的群消息（`GROUP_MESSAGE_CREATE` / `GROUP_AT_MESSAGE_CREATE`）都会用 `author.member_role` **实时刷新缓存**，与是否命中任何指令无关；升/降管后对方再发一条就会改写。平台无实时查任意成员身份的公开接口。 |
| 模版消息 | — | 已淘汰，不做 |

自定义 keyboard 官方可能要求内邀；未开通时发送会失败。

### 发消息内容标记（写在内容字符串里）

| 写法 | 含义 |
|------|------|
| `[图片,url:网址]` / `[图片,本地:路径]` | 图片；URL 或本地分片上传；可与文字同条 |
| `[语音,url:网址]` / `[语音,本地:路径]` | 语音（`file_type=3`）；**单发不挂文** |
| `[视频,url:网址]` / `[视频,本地:路径]` | 视频（`file_type=2`，mp4）；**单发不挂文**；封面由客户端抽帧，**不能**自定义 |
| `[文件,url:网址]` / `[文件,本地:路径]` | 文件（`file_type=4`）；**单发不挂文** |
| 上述标记末尾 `,显示名` | 可选；写入上传 `file_name`（文件卡片显示名最常用；URL 有自定义名时会先下载再分片） |

说明：

- 官方**没有引用气泡**；被动回复由框架自动带 `msg_id`，内容里不必写 `[引用]`（若仍写会被剥离、不进正文）
- 上述标记仅支持 `url` / `本地`（可选尾部 `,显示名`）；其它写法会报错；数量不限
- **本地路径**相对**插件根目录**（不是相对 `.ka`），资源放 `数据文件/`，构建会打包
- 官方每条 `msg_type=7` 最多 1 个富媒体；**图片**可带说明文字；**语音/视频/文件**前后文字会另拆文本消息，不会挂到媒体条上
- 同条图文时客户端可能把图片置顶显示
- 标记要写在内容里（引号 / 反引号内），不要当成独立语句

```text
发消息("上文[图片,url:https://example.com/a.png]下文")
发消息(`[图片,url:https://q.qlogo.cn/qqapp/{AppID}/{发言人}/100]`)
发消息(`[语音,本地:数据文件/将军.mp3]`)
发消息(`[视频,本地:数据文件/70.mp4]`)
发消息(`[文件,本地:数据文件/404.html,自定义说明.html]`)

[临时定义] 出 = `[图片,本地:数据文件/cover.png]`
出 += `\n本地相对路径发图`
发消息(出)
```

### 运算与注释

| 写法 | 含义 |
|------|------|
| `+` `-` `*` `/` | 算式（`*` `/` 优先；可用括号；除数为 0 时结果为 0） |
| `名字++` | 数字自增 1（`+=` 仍是字符串拼接） |
| `名字 += 值` | 字符串拼接（支持 `\n`） |
| `"文字" [插件定义:名]` | 字符串与变量紧挨拼接（也可用 `+`；更推荐 `"…{名}"`） |
| `==` `!=` | 内容相等比较（按文本） |
| `>` `>=` `<` `<=` / `＜` `＞` | 比大小：两侧都是**纯数字**才比较；否则条件不成立 |
| `表达式.match(/正则/flags)` | 类 JS 正则匹配；条件里匹配成功为真，并供后面的 `[取括号]` / `{取括号}` 使用 |
| `#` / `//` / `；` | 注释 |

```text
# 不含 ^$：正文里包含即可
[如果]([文本消息].match(/测试7(.*)/)){
    发消息("括号内容={取括号:1}")
}

# 有 ^：必须以这段开头
[如果]([文本消息].match(/^测试7-2(.*)/)){
    发消息("括号内容={取括号:1}")
}

# 有 ^ 和 $：开头到结尾整段都要对上
[如果]([文本消息].match(/^测试7-3(.*)$/)){
    发消息("括号内容={取括号:1}")
}

# 多个 ()：用 {取括号:1} {取括号:2} …
[如果]([文本消息].match(/测试7-1 (.*) (.*)/)){
    发消息("{取括号:1} / {取括号:2}")
}
```

```text
[临时定义] i = 0
[临时定义] n = [取艾特数]
[临时定义] 出 = `艾特数={n}\n列表：`
[For循环](i ＜ n){
    出 += `\n[{i}]={取艾特:i}`
    i++
}
```

---

## 值的写法

| 写法 | 类型 |
|------|------|
| `111`（无引号） | 数字 |
| `true` / `false`（或 `真` / `假`） | 布尔；可与回执字段比：`回[成功] == true` |
| `"111"` 或反引号 `` `111` `` | 字符串（两种引号等价，都支持 `{插值}`） |
| `1 + 2 * 3` / `(1+2)*3` | 算式（结果为数字） |
| `变量名` | 读取你定义的变量 |
| `[文本消息]` 等 | 底层内置值 |

---

## 可自定义（非核心名）

| 项 | 说明 |
|----|------|
| `.ka` 文件名 | 任意，扩展名 `.ka` |
| `[全局定义]` / `[临时定义]` 后的**变量名** | 自己起名 |
| 字符串内容、比对文本、openid 等 | 内容自定 |
